<div id="pir-container">
    <section id="pir-results-header">
        <h1>Compatibility Check Results</h1>
    </section>
    <section id="pir-results-body">
        <div class="pir-table">
            <div class="pir-table-header">
                <div class="pir-table-cell">Actor</div>
                <div class="pir-table-cell">Type</div>
                <div class="pir-table-cell">Name</div>
                <div class="pir-table-cell">Actions</div>
                <div class="pir-table-cell">Changed Property</div>
                <div class="pir-table-cell">Actor Item Link</div>
                <div class="pir-table-cell">Origin Item Link</div>
                <div class="pir-table-cell">Notes</div>
            </div>

        {{#each items}}
            <div class="pir-table-row">
                <div class="pir-table-cell">{{{this.actorLink}}}</div>
                <div class="pir-table-cell">{{this.type}}</div>
                <div class="pir-table-cell">{{this.name}}</div>
                <div class="pir-table-cell pir-button-holder">
                    {{#unless this.unrevitalizable}} <!-- We are allowed to use Revitalize function -->
                    <button class="pir-button" onclick="revitalize('{{this.uuid}}', '{{{this.comparativeData}}}') && removeRow(this);" title="Revitalize listed properties from Compendium. WARNING, use at own risk!">
                        <i class="fa-solid fa-code-compare"></i>
                    </button>
                    {{else}} <!-- We are not allowed to use Revitalize function -->
                    <button class="pir-button pir-button-disabled" disabled="true" title="{{this.unrevitalizable}}">
                        <i class="fa-solid fa-code-compare"></i>
                    </button>
                    {{/unless}}
                    <button class="pir-button" onclick="hide('{{this.uuid}}') && removeRow(this);" title="Hide this Item in the future">
                        <i class="fa-regular fa-eye-slash"></i>
                    </button>
                    <button class="pir-button" onclick="removeRow(this);" title="Remove Item from list">
                        <i class="fa-regular fa-check" ></i>
                    </button>
                </div>
                <div class="pir-table-cell">{{{this.comparativeDataText}}}</div>
                <div class="pir-table-cell">{{{this.actorItemLink}}}</div>
                <div class="pir-table-cell">{{{this.originItemLink}}}</div>
                <div class="pir-table-cell">{{this.notes}}</div>
            </div>
        {{/each}}
        </div>
    </section>